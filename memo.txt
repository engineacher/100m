						var td = jS.getTd(0, 0, 0);
						td = jQuery(td);
						jS.cellEdit(td);
						
$("#debug1").text(r);

margin-top: -1em;

	width: inherit ! important;
	height: inherit ! important;
	
	height: auto ! important;
	

タイマーを実装する
まず、はじめのアニメーションを実装
